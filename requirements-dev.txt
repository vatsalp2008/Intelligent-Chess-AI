# Minimal set needed to run the engines and their unit tests.
#
# The full requirements.txt pins the whole environment (Flask, matplotlib,
# PyInstaller, Ollama) which the web UIs and LLM bots need. For working on
# the search or running the tests, this is enough:
#
#     pip install -r requirements-dev.txt
#     cd classic_agent && python -m unittest test_evaluation
#     cd llm_agent && python -m unittest test_knightmare

chess==1.11.2

# The web interface tests drive Flask through its test client, so add it if
# you want run_tests.sh to cover them rather than skip them:
#
#     pip install flask

# benchmark_stockfish.py additionally needs a Stockfish binary on PATH,
# which is not a Python package:
#
#     brew install stockfish          # macOS
#     sudo apt-get install stockfish  # Debian/Ubuntu
#
# Set STOCKFISH_PATH if it lives somewhere unusual. Everything else,
# including the whole test suite, runs without it.
